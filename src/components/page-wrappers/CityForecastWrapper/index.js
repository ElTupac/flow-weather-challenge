const CityForecastWrapper = ({ city }) => (
  <div className="container mx-auto flex flex-col md:flex-row md:justify-center md:items-center gap-5">
    <p>{city}</p>
  </div>
);

export default CityForecastWrapper;
